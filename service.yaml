apiVersion: v1
kind: Service

metadata:
  name: devops-nginx-service
  labels:
    app: devops-nginx

spec:
  type: NodePort

  selector:
    app: devops-nginx

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
